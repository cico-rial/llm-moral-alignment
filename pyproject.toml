[project]
name = "synthetic-generation-alignment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "accelerate>=1.13.0",
    "bitsandbytes>=0.49.2",
    "peft>=0.18.1",
    "torch==2.8.0",
    "trl==0.11.0",
]
